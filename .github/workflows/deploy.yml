name: Fix and Deploy

on:
  push:
    branches:
      - master
      - main
    paths-ignore:
      - README.md

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    uses: ggrabarz/.github/.github/workflows/fix-and-deploy.yml@master